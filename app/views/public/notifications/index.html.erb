<div class='my-5', id="title">
		<p>
		<span>通</span>
		<span>知</span>
	</p>
</div>
<% notifications = @notifications.where.not(visitor_id: current_user.id)  %>
<% if notifications.exists? %>
    <%= render notifications %> <%= paginate notifications %> 
<% else %>
    <p style="text-align: center;"> 通知はありません </p>
<% end %>