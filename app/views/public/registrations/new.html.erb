<div class="col-md-6 col-sm-10 mx-2 mx-sm-auto shadow-lg rounded px-0 my-5 registrations" style="text-align: center;">
	<div class='pt-5', id="title">
		<p>
			<span>S</span>
			<span>I</span>
			<span>G</span>
			<span style ="margin-right:20px;">N</span>
			<span>U</span>
			<span>P</span>
		</p>
	</div>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <p><%= render "public/shared/error_messages", resource: resource %><p>

  <div class="field mb-3">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'search_container' %>
  </div>

  <div class="field mb-3">
    <%= f.label :パスワード %>
    <% if @minimum_password_length %>
    <em>（<%= @minimum_password_length %>文字以上）</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password", class: 'search_container' %>
  </div>

  <div class="field mb-3">
  <%= f.label :パスワード（確認用） %><br />
  <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'search_container' %>
  </div>
  <div class="field">
    <%= f.label :ユーザーネーム %><br />
    <%= f.text_field :user_name, autofocus: true, autocomplete: "user_name", class: 'search_container' %>
  </div>
<!--  <div class="field">
    <%#= f.label :誕生日 %><br />
    <%#== sprintf(f.date_select(:birth, :use_month_numbers=>true, :date_separator => '%s', :start_year=>1930, :end_year => Time.now.year, :default =>'1990-1-1'.to_date), '年','月') + '日生まれ' %>
  </div>-->
  <div class="actions">
    <%= f.submit "アカウント作成" ,class: "btn btn-success  my-5" %>
  </div>
<% end %>
</div>
<%#= render "public/shared/links" %>
